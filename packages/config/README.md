# @cyphra/config

Loads **`cyphra.config.ts`** (or **`cyphra.json`**) and resolves **absolute paths** for `schema` and `migrations`.

Used by **`@cyphra/cli`** and **`loadCyphraWorkspace`** in the **`cyphra`** meta-package.
